<%= form_for(@link, remote: true, :id => "link_form") do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.text_field :given_url, :placeholder => "Paste a link to shorten", :id => "link_content", :class => "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit "Get Lop'd", :class => "btn btn-default" %>
  </div>
<% end %>
